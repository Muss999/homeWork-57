export interface TypeUser {
    name: string;
    email: string;
    isActive: boolean;
    role: string;
    id: string;
}

export interface TypeUserMutation {
    name: string;
    email: string;
    isActive: boolean;
    role: string;
}
